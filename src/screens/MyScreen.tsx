import React from 'react';
import { View, SafeAreaView, Text } from 'react-native';

const MyScreen = () => {
    return (
        <SafeAreaView>
            <Text>MyScreen</Text>
        </SafeAreaView>
    );
};

export default MyScreen;
