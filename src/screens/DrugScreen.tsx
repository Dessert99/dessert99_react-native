import React from 'react';
import { View, Text, SafeAreaView } from 'react-native';

const DrugScreen = () => {
    return (
        <SafeAreaView>
            <Text>DrugScreen</Text>
        </SafeAreaView>
    );
};

export default DrugScreen;
