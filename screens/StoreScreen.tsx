import { View, SafeAreaView, Text } from 'react-native';

const StoreScreen = () => {
    return (
        <SafeAreaView>
            <Text>스토어 뷰</Text>
        </SafeAreaView>
    );
};

export default StoreScreen;
